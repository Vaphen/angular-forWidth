import { ForWidthDirective } from './for-width.directive';

describe('ForWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new ForWidthDirective();
    expect(directive).toBeTruthy();
  });
});
